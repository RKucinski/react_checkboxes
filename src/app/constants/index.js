import colors from './Colors';
import styles from './Styles';

export const ColorsConstant = colors;
export const StylesConstant = styles;

// At the moment, there are only 2 files in Constants so we might discuss that index.
// But as soon as the app grows it'll become more usefull
